# Required: MiniMax API key (used by the `default` tier).
MINIMAX_API_KEY=

# Optional: needed if you uncomment the premium tier in litellm.yaml.
# ANTHROPIC_API_KEY=

# Note: the `gh` CLI handles its own GitHub auth via `gh auth login`.
# No env var is needed here for GitHub.
